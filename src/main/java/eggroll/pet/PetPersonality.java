package eggroll.pet;

public enum PetPersonality {
    // TODO: Figure it out
    LAZY, // has a higher cap for energy, because always rested?
    PLAYFUL, // has a higher cap for happiness, because whimsical
    FOODIE, // has a higher cap for hunger, because loves to eat a lot
    WELL_GROOMED, // has a higher cap for hygiene, because loves to stay clean
    SWOLE, // has a higher cap for fitness, because enjoys bodybuilding.
}
